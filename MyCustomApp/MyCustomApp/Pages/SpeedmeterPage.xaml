<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MyCustomApp.Pages.SpeedmeterPage"
             Title="Speed Meter"
             BackgroundColor="Black">

    <Grid VerticalOptions="FillAndExpand" HorizontalOptions="FillAndExpand">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- 속도 표시 라벨 -->
        <Grid Grid.Row="0" Grid.Column="0" HorizontalOptions="Center">
            <Label x:Name="SpeedLabel" Text="0 km/h" TextColor="#D0D0D0" FontSize="55" HorizontalOptions="Center" VerticalOptions="Start" Margin="0,50,0,0" Grid.Row="0" />
        </Grid>

        <!-- 기울기 표시 라벨 -->
        <StackLayout Grid.Row="1" Grid.Column="0" HorizontalOptions="Center">
            <Label x:Name="InclineLabel" Text="0.0 %" TextColor="#D0D0D0" FontSize="55" HorizontalOptions="Center" VerticalOptions="Start" Margin="0,30,0,0" Grid.Row="0" />
        </StackLayout>
    </Grid>
</ContentPage>
